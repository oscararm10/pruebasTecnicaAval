# Copia este archivo a .env y ajusta la URL del API.
# Vacío = usa proxy local http://localhost:4000
# AWS ejemplo:
# API_BASE_URL=https://xxxx.execute-api.us-east-1.amazonaws.com/Prod

API_BASE_URL=
